--- a/boards/tangnano9k/ascon_aead128_axis_slow/Makefile	2026-05-17 07:10:16.000000000 +0000
+++ b/boards/tangnano9k/ascon_aead128_axis_slow/Makefile	2026-05-17 07:53:35.683024884 +0000
@@ -5,6 +5,11 @@
 CST      := tangnano9k_ascon_aead128_axis_slow.cst
 BUILD    := build
 
+YOSYS           ?= $(shell command -v yowasp-yosys 2>/dev/null || command -v yosys 2>/dev/null || echo yosys)
+NEXTPNR         ?= $(shell command -v nextpnr-gowin 2>/dev/null || command -v yowasp-nextpnr-himbaechel-gowin 2>/dev/null || command -v nextpnr-himbaechel 2>/dev/null || echo nextpnr-himbaechel)
+GOWIN_PACK      ?= gowin_pack
+OPENFPGALOADER  ?= openFPGALoader
+
 COMMON_RTL := ../../../rtl/common
 SOURCES := \
   $(COMMON_RTL)/ascon_accel_regs.vh \
@@ -24,32 +29,36 @@
 all: $(FS)
 
 tools:
-	@command -v yosys >/dev/null
-	@command -v nextpnr-gowin >/dev/null || command -v nextpnr-himbaechel >/dev/null
-	@command -v gowin_pack >/dev/null
-	@command -v openFPGALoader >/dev/null
+	@echo "YOSYS=$(YOSYS)"
+	@echo "NEXTPNR=$(NEXTPNR)"
+	@echo "GOWIN_PACK=$(GOWIN_PACK)"
+	@echo "OPENFPGALOADER=$(OPENFPGALOADER)"
+	@which $(YOSYS)
+	@which $(NEXTPNR)
+	@which $(GOWIN_PACK)
+	@which $(OPENFPGALOADER)
 
 $(BUILD):
 	mkdir -p $(BUILD)
 
 $(JSON): $(SOURCES) | $(BUILD)
-	yosys -q -p "read_verilog -I$(COMMON_RTL) $(SOURCES); synth_gowin -top $(TOP) -json $(JSON)"
+	$(YOSYS) -q -p "read_verilog -I$(COMMON_RTL) $(SOURCES); synth_gowin -top $(TOP) -json $(JSON)"
 
 $(PNR_JSON): $(JSON) $(CST)
-	@if command -v nextpnr-gowin >/dev/null; then \
-	  nextpnr-gowin --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --family $(FAMILY) --cst $(CST); \
+	@if [ "$$(basename "$(NEXTPNR)")" = "nextpnr-gowin" ]; then \
+	  $(NEXTPNR) --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --family $(FAMILY) --cst $(CST); \
 	else \
-	  nextpnr-himbaechel --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --vopt family=$(FAMILY) --vopt cst=$(CST); \
+	  $(NEXTPNR) --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --vopt family=$(FAMILY) --vopt cst=$(CST); \
 	fi
 
 $(FS): $(PNR_JSON)
-	gowin_pack -d $(FAMILY) -o $(FS) $(PNR_JSON)
+	$(GOWIN_PACK) -d $(FAMILY) -o $(FS) $(PNR_JSON)
 
 prog-sram: $(FS)
-	openFPGALoader -b $(BOARD) $(FS)
+	$(OPENFPGALOADER) -b $(BOARD) $(FS)
 
 prog-flash: $(FS)
-	openFPGALoader -b $(BOARD) -f $(FS)
+	$(OPENFPGALOADER) -b $(BOARD) -f $(FS)
 
 clean:
 	rm -rf $(BUILD)
--- a/boards/tangnano9k/ascon_aead128_mmio_slow/Makefile	2026-05-17 07:10:16.000000000 +0000
+++ b/boards/tangnano9k/ascon_aead128_mmio_slow/Makefile	2026-05-17 07:53:35.684240832 +0000
@@ -5,6 +5,11 @@
 CST      := tangnano9k_ascon_aead128_mmio_slow.cst
 BUILD    := build
 
+YOSYS           ?= $(shell command -v yowasp-yosys 2>/dev/null || command -v yosys 2>/dev/null || echo yosys)
+NEXTPNR         ?= $(shell command -v nextpnr-gowin 2>/dev/null || command -v yowasp-nextpnr-himbaechel-gowin 2>/dev/null || command -v nextpnr-himbaechel 2>/dev/null || echo nextpnr-himbaechel)
+GOWIN_PACK      ?= gowin_pack
+OPENFPGALOADER  ?= openFPGALoader
+
 COMMON_RTL := ../../../rtl/common
 SOURCES := \
   $(COMMON_RTL)/ascon_accel_regs.vh \
@@ -23,32 +28,36 @@
 all: $(FS)
 
 tools:
-	@command -v yosys >/dev/null
-	@command -v nextpnr-gowin >/dev/null || command -v nextpnr-himbaechel >/dev/null
-	@command -v gowin_pack >/dev/null
-	@command -v openFPGALoader >/dev/null
+	@echo "YOSYS=$(YOSYS)"
+	@echo "NEXTPNR=$(NEXTPNR)"
+	@echo "GOWIN_PACK=$(GOWIN_PACK)"
+	@echo "OPENFPGALOADER=$(OPENFPGALOADER)"
+	@which $(YOSYS)
+	@which $(NEXTPNR)
+	@which $(GOWIN_PACK)
+	@which $(OPENFPGALOADER)
 
 $(BUILD):
 	mkdir -p $(BUILD)
 
 $(JSON): $(SOURCES) | $(BUILD)
-	yosys -q -p "read_verilog -I$(COMMON_RTL) $(SOURCES); synth_gowin -top $(TOP) -json $(JSON)"
+	$(YOSYS) -q -p "read_verilog -I$(COMMON_RTL) $(SOURCES); synth_gowin -top $(TOP) -json $(JSON)"
 
 $(PNR_JSON): $(JSON) $(CST)
-	@if command -v nextpnr-gowin >/dev/null; then \
-	  nextpnr-gowin --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --family $(FAMILY) --cst $(CST); \
+	@if [ "$$(basename "$(NEXTPNR)")" = "nextpnr-gowin" ]; then \
+	  $(NEXTPNR) --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --family $(FAMILY) --cst $(CST); \
 	else \
-	  nextpnr-himbaechel --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --vopt family=$(FAMILY) --vopt cst=$(CST); \
+	  $(NEXTPNR) --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --vopt family=$(FAMILY) --vopt cst=$(CST); \
 	fi
 
 $(FS): $(PNR_JSON)
-	gowin_pack -d $(FAMILY) -o $(FS) $(PNR_JSON)
+	$(GOWIN_PACK) -d $(FAMILY) -o $(FS) $(PNR_JSON)
 
 prog-sram: $(FS)
-	openFPGALoader -b $(BOARD) $(FS)
+	$(OPENFPGALOADER) -b $(BOARD) $(FS)
 
 prog-flash: $(FS)
-	openFPGALoader -b $(BOARD) -f $(FS)
+	$(OPENFPGALOADER) -b $(BOARD) -f $(FS)
 
 clean:
 	rm -rf $(BUILD)
--- a/boards/tangnano9k/ascon_aead128_axis128_4rpc/Makefile	2026-05-17 07:10:16.000000000 +0000
+++ b/boards/tangnano9k/ascon_aead128_axis128_4rpc/Makefile	2026-05-17 07:53:35.685206828 +0000
@@ -5,6 +5,11 @@
 CST      := tangnano9k_ascon_aead128_axis128_4rpc.cst
 BUILD    := build
 
+YOSYS           ?= $(shell command -v yowasp-yosys 2>/dev/null || command -v yosys 2>/dev/null || echo yosys)
+NEXTPNR         ?= $(shell command -v nextpnr-gowin 2>/dev/null || command -v yowasp-nextpnr-himbaechel-gowin 2>/dev/null || command -v nextpnr-himbaechel 2>/dev/null || echo nextpnr-himbaechel)
+GOWIN_PACK      ?= gowin_pack
+OPENFPGALOADER  ?= openFPGALoader
+
 COMMON_RTL := ../../../rtl/common
 SOURCES := \
   $(COMMON_RTL)/ascon_accel_regs.vh \
@@ -25,32 +30,36 @@
 all: $(FS)
 
 tools:
-	@command -v yosys >/dev/null
-	@command -v nextpnr-gowin >/dev/null || command -v nextpnr-himbaechel >/dev/null
-	@command -v gowin_pack >/dev/null
-	@command -v openFPGALoader >/dev/null
+	@echo "YOSYS=$(YOSYS)"
+	@echo "NEXTPNR=$(NEXTPNR)"
+	@echo "GOWIN_PACK=$(GOWIN_PACK)"
+	@echo "OPENFPGALOADER=$(OPENFPGALOADER)"
+	@which $(YOSYS)
+	@which $(NEXTPNR)
+	@which $(GOWIN_PACK)
+	@which $(OPENFPGALOADER)
 
 $(BUILD):
 	mkdir -p $(BUILD)
 
 $(JSON): $(SOURCES) | $(BUILD)
-	yosys -q -p "read_verilog -I$(COMMON_RTL) $(SOURCES); synth_gowin -top $(TOP) -json $(JSON)"
+	$(YOSYS) -q -p "read_verilog -I$(COMMON_RTL) $(SOURCES); synth_gowin -top $(TOP) -json $(JSON)"
 
 $(PNR_JSON): $(JSON) $(CST)
-	@if command -v nextpnr-gowin >/dev/null; then \
-	  nextpnr-gowin --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --family $(FAMILY) --cst $(CST); \
+	@if [ "$$(basename "$(NEXTPNR)")" = "nextpnr-gowin" ]; then \
+	  $(NEXTPNR) --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --family $(FAMILY) --cst $(CST); \
 	else \
-	  nextpnr-himbaechel --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --vopt family=$(FAMILY) --vopt cst=$(CST); \
+	  $(NEXTPNR) --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --vopt family=$(FAMILY) --vopt cst=$(CST); \
 	fi
 
 $(FS): $(PNR_JSON)
-	gowin_pack -d $(FAMILY) -o $(FS) $(PNR_JSON)
+	$(GOWIN_PACK) -d $(FAMILY) -o $(FS) $(PNR_JSON)
 
 prog-sram: $(FS)
-	openFPGALoader -b $(BOARD) $(FS)
+	$(OPENFPGALOADER) -b $(BOARD) $(FS)
 
 prog-flash: $(FS)
-	openFPGALoader -b $(BOARD) -f $(FS)
+	$(OPENFPGALOADER) -b $(BOARD) -f $(FS)
 
 clean:
 	rm -rf $(BUILD)
--- a/boards/tangnano9k/ascon_aead128_axis128_8rpc/Makefile	2026-05-17 07:10:16.000000000 +0000
+++ b/boards/tangnano9k/ascon_aead128_axis128_8rpc/Makefile	2026-05-17 07:53:35.686279696 +0000
@@ -5,6 +5,11 @@
 CST      := tangnano9k_ascon_aead128_axis128_8rpc.cst
 BUILD    := build
 
+YOSYS           ?= $(shell command -v yowasp-yosys 2>/dev/null || command -v yosys 2>/dev/null || echo yosys)
+NEXTPNR         ?= $(shell command -v nextpnr-gowin 2>/dev/null || command -v yowasp-nextpnr-himbaechel-gowin 2>/dev/null || command -v nextpnr-himbaechel 2>/dev/null || echo nextpnr-himbaechel)
+GOWIN_PACK      ?= gowin_pack
+OPENFPGALOADER  ?= openFPGALoader
+
 COMMON_RTL := ../../../rtl/common
 SOURCES := \
   $(COMMON_RTL)/ascon_accel_regs.vh \
@@ -25,32 +30,36 @@
 all: $(FS)
 
 tools:
-	@command -v yosys >/dev/null
-	@command -v nextpnr-gowin >/dev/null || command -v nextpnr-himbaechel >/dev/null
-	@command -v gowin_pack >/dev/null
-	@command -v openFPGALoader >/dev/null
+	@echo "YOSYS=$(YOSYS)"
+	@echo "NEXTPNR=$(NEXTPNR)"
+	@echo "GOWIN_PACK=$(GOWIN_PACK)"
+	@echo "OPENFPGALOADER=$(OPENFPGALOADER)"
+	@which $(YOSYS)
+	@which $(NEXTPNR)
+	@which $(GOWIN_PACK)
+	@which $(OPENFPGALOADER)
 
 $(BUILD):
 	mkdir -p $(BUILD)
 
 $(JSON): $(SOURCES) | $(BUILD)
-	yosys -q -p "read_verilog -I$(COMMON_RTL) $(SOURCES); synth_gowin -top $(TOP) -json $(JSON)"
+	$(YOSYS) -q -p "read_verilog -I$(COMMON_RTL) $(SOURCES); synth_gowin -top $(TOP) -json $(JSON)"
 
 $(PNR_JSON): $(JSON) $(CST)
-	@if command -v nextpnr-gowin >/dev/null; then \
-	  nextpnr-gowin --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --family $(FAMILY) --cst $(CST); \
+	@if [ "$$(basename "$(NEXTPNR)")" = "nextpnr-gowin" ]; then \
+	  $(NEXTPNR) --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --family $(FAMILY) --cst $(CST); \
 	else \
-	  nextpnr-himbaechel --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --vopt family=$(FAMILY) --vopt cst=$(CST); \
+	  $(NEXTPNR) --json $(JSON) --write $(PNR_JSON) --freq 27 --device $(DEVICE) --vopt family=$(FAMILY) --vopt cst=$(CST); \
 	fi
 
 $(FS): $(PNR_JSON)
-	gowin_pack -d $(FAMILY) -o $(FS) $(PNR_JSON)
+	$(GOWIN_PACK) -d $(FAMILY) -o $(FS) $(PNR_JSON)
 
 prog-sram: $(FS)
-	openFPGALoader -b $(BOARD) $(FS)
+	$(OPENFPGALOADER) -b $(BOARD) $(FS)
 
 prog-flash: $(FS)
-	openFPGALoader -b $(BOARD) -f $(FS)
+	$(OPENFPGALOADER) -b $(BOARD) -f $(FS)
 
 clean:
 	rm -rf $(BUILD)
